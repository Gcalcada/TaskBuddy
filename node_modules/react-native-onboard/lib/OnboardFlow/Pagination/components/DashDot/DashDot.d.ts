/// <reference types="react" />
export declare function DashDot({ selected, paginationSelectedColor, paginationColor }: {
    selected: boolean;
    paginationSelectedColor: string;
    paginationColor: string;
}): JSX.Element;
