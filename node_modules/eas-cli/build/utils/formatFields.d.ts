declare type FormatFieldsOptions = {
    labelFormat: (raw: string) => string;
};
export default function formatFields(fields: {
    label: string;
    value: string;
}[], options?: FormatFieldsOptions): string;
export {};
