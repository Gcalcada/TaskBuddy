import { ExpoConfig } from '@expo/config';
import { Env } from '@expo/eas-build-job';
interface Options {
    env?: Env;
    isPublicConfig?: boolean;
}
export declare function getExpoConfig(projectDir: string, opts?: Options): ExpoConfig;
export {};
