import { AppPrivacy } from '../generated';
export declare const AppMutation: {
    createAppAsync(appInput: {
        accountId: string;
        projectName: string;
        privacy: AppPrivacy;
    }): Promise<string>;
};
