/// <reference types="react" />
export declare function Dot({ selected, paginationSelectedColor, paginationColor }: {
    selected: boolean;
    paginationSelectedColor: string;
    paginationColor: string;
}): JSX.Element;
